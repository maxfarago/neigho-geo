{
  "name": "neigho-geo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "db:migrate": "wrangler d1 migrations apply neigho-geo --local",
    "db:migrate:remote": "wrangler d1 migrations apply neigho-geo --remote",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.56.0",
    "vite": "^8.2.2",
    "wrangler": "^4.135.0"
  }
}
